name: zam_factory_pattern
version: 0.2.0
description: Provides utilities to implement the factory pattern which deals
  with the problem of creating objects.
homepage: https://zamstation.com
repository: https://github.com/zamstation/zam_factory_pattern

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  zam_core: ^0.6.0

dev_dependencies:
  lints: ^1.0.1
  zam_test: ^0.4.0
